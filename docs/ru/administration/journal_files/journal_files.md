# {heading(Журналы событий OpenSearch)[id=journal_files]}

{var(sys1)} осуществляет логирование или сбор записей о событиях в соответствующие журналы.

## {heading(Файлы журналов (логов))[id=log_files]}

Поскольку {var(sys1)} представляет собой распределённую систему, состоящую из множества компонентов, то журналы (логи) могут храниться в разных файлах и даже на разных серверах.

Основные лог-файлы хранятся в директории `/var/log/infra` (на управляющих и вычислительных узлах). В директории `/var/log/infra-latest` находится ссылка на последний лог-файл из директории `/var/log/infra`.

<info>

Логи docker-контейнеров записываются в `/var/log/containers/`.

</info>

## {heading(Анализ записей журналов (логов))[id=analyzing_log_entries]}

Все журналы собираются с использованием Fluentd в OpenSearch подсистемы логирования. В качестве интерфейса для централизованной обработки и анализа записей о событиях используется Портал логирования (OpenSearch Dashboards).

Портал логирования позволяет оперировать поисковыми запросами на одном из поддерживаемых языков:

* **Apache Lucene** — поисковый запрос формируется из условий, которые разделяются ключевыми словами `AND` или `OR`. Текст заключается в двойные кавычки (" ").
* **Dashboards Query Language** — поисковый запрос формируется из параметров и значений, которые разделяются `:` (подробнее — [в официальной документации OpenSearch](https://opensearch.org/docs/1.3/dashboards/discover/dql/)).

Для поиска записей событий в Портале логирования:

1. Перейдите на страницу **Discover**.
1. В выпадающем списке, расположенном под кнопкой **Add filter**, выберите шаблон индекса.
1. Нажмите кнопку **Add filter** и добавьте фильтры.
1. В текстовом поле **Search** введите поисковый запрос.
1. Нажмите ENTER для запуска поиска.

Для получения полной информации о событии, включая представление в виде JSON, нажмите на значок **>** слева от результата запроса.

Если в результате выполнения запроса не обнаружены записи о событиях, попробуйте увеличить интервал времени поиска событий.

Основные шаблоны индексов событий приведены в {linkto(#tab_event_index_templates)[text=таблице %number]}.

{caption(Таблица {counter(table)[id=numb_tab_event_index_templates]} — Шаблоны индексов событий)[align=right;position=above;id=tab_event_index_templates;number={const(numb_tab_event_index_templates)}]}
[cols="1,2", options="header"]
|===
|Шаблон индекса
|Расшифровка

|`*_cinder-*`
|События компонента Cinder (резервное копирование).

|`filebeat-7.8.0-20*`
|События всех компонентов в Kubernetes. Создаётся по умолчанию (подробнее — в [официальной документации Elasticsearch](https://www.elastic.co/guide/en/beats/filebeat/current/change-index-name.html))

|`filebeat-*`
|События всех компонентов {var(sys2)} (с хостов и Kubernetes). Включает в себя данные по индексу `filebeat-7.8.0-20*`

|`*_glance-*`
|События компонента Glance (работа с образами)

|`*_haproxy-*`
|События компонента HAProxy (балансировка нагрузки)

|`*_heat-*`
|События компонента Heat (оркестрация)

|`*_iam-*`
|События компонента IAM (биллинг, ролевая модель)

|`*_keystone-*`
|События компонента Keystone (аутентификация)

|`*_magnum-*`
|События компонента Magnum (оркестрация контейнеров)

|`*_neutron-*`
|События компонента Neutron (сети)

|`*_nova-*`
|События компонента Nova (виртуализация)

|`*_scrooge-*`
|События компонента Scrooge (биллинг и квоты)

|`*_trove-*`
|События компонента Trove (инстансы СУБД)

|`*_audit-*`
|События безопасности {var(sys2)}
|===
{/caption}

<info>

Полный список доступных индексов перечислен в разделе **Index Management** → **Indices**. Данные индексы доступны в формате `ndjson` на деплой-ноде в репозитории `/home/redos/inventory/ansible-openstack/roles/opensearch/files/saved_objects/` (с суффиксом `01-index-pattern`).

</info>