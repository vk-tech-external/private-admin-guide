# {heading(Функциональная структура)[id=arch-structure]}

Структура {var(sys2)} предусматривает разделение всего объёма функций на логические блоки — подсистемы. Перечень подсистем представлен в {linkto(#tab_subsystem_list)[text=таблице %number]}.

{caption(Таблица {counter(table)[id=numb_tab_subsystem_list]} — Список подсистем {var(sys2)})[align=right;position=above;id=tab_subsystem_list;number={const(numb_tab_subsystem_list)}]}
[cols="1,3", options="header"]
|===
{ifdef(box)}
2+|Интерфейсы

|Подсистема потребителя облачных услуг
|Предназначена для предоставления графического интерфейса, позволяющего пользователю взаимодействовать со всеми подсистемами {var(sys2)}

|Подсистема администрирования
|Предназначена для предоставления графического интерфейса, позволяющего администратору взаимодействовать со всеми подсистемами {var(sys2)}. В состав подсистемы входят решения для мониторинга и сбора логов

2+|**Группа подсистем PaaS/SaaS**

|Подсистема управляемых баз данных
|Предназначена для автоматизации развёртывания СУБД

|Подсистема управляемых контейнеров
|Предназначена для автоматизации управления кластерами вычислительных контейнеров Kubernetes

|Подсистема магазина приложений Marketplace
|Предназначена для предоставления пользователям {var(sys2)} доступа к сторонним сервисам разного рода:

* Предоставляемые поставщиком.
* Opensource-решения.
* Сервисы собственной разработки.

Все детали по разворачиванию инфраструктуры и ПО или созданию тенанта/аккаунта остаются на стороне {var(sys2)}. Пользователь получает подготовленный экземпляр сервиса (инстанс сервиса), который при необходимости может донастроить под свои нужды. Все развёрнутые инстансы сервиса тарифицируются и управляются в рамках проекта {var(sys2)}. Marketplace помогает быстрее запускать сервисы и упрощает их поддержку

2+|**Группа подсистем IaaS**

|Подсистема вычислительных ресурсов
|Предназначена для управления вычислительными узлами {var(sys2)} и виртуальными машинами

|Подсистема хранения данных
|Предназначена для оркестрации конечных решений по блочному хранению данных и хранения образов операционных систем

|Сетевая подсистема
|Предназначена для реализации и управления всеми аспектами виртуальной сетевой инфраструктуры

|Подсистема служебных хранилищ
|Предназначена для хранения данных настроек {var(sys2)}, информации о ВМ и других внутренних данных {var(sys2)}

2+|**Общие подсистемы**

|Подсистема тарификации и биллинга
|Предназначена для сбора информации о потребляемых ресурсах и учёт использования потребляемых ресурсов {var(sys2)} с точностью до минуты по модели «Pay-As-You-Go» (биллинг по факту использования)

|Подсистема резервного копирования
|Предназначена для резервного копирования сущностей {var(sys2)}
{/ifdef}

{ifndef(box)}
|Подсистема
|Краткое описание

|Подсистема управления доступом
|Предназначена для идентификации, аутентификации и авторизации пользователей и всех смежных сервисов между собой

|Подсистема вычислительных ресурсов
|Предназначена для управления вычислительными узлами {var(sys2)} и виртуальными машинами

|Подсистема программно-определяемой сети
|Предназначена для управления сетевой инфраструктурой

|Подсистема хранения образов
|Предназначена для хранения образов виртуальных машин и метаданных

|Подсистема хранения данных
|Предназначена для оркестрации конечных решений по блочному хранению данных

|Подсистема резервного копирования
|Предназначена для создания копий виртуальных машин и их метаданных для последующего восстановления

|Подсистема управления квотами
|Предназначена для управления, дистрибьюции и предоставления информации о текущих квотах для других подсистем платформы

|Подсистема управления биллингом
|Предназначена для сбора информации о потребляемых ресурсах и учёта использования потребляемых ресурсов {var(sys2)} с точностью до минуты по модели «Pay-As-You-Go» (биллинг по факту использования)

|Подсистема интерфейса пользователя
|Предназначена для создания и управления пользователями {var(sys2)} виртуальными вычислительными ресурсами в рамках проекта или проектов

|Подсистема интерфейса администратора
|Предназначена для выполнения администратором {var(sys2)} операций по обслуживанию прикладного ПО и сервисов {var(sys2)}, настройке и диагностированию {var(sys2)}, анализу метрик и журналов работы компонентов {var(sys2)}
{/ifndef}
|===
{/caption}

Для реализации указанных подсистем используется совокупность множества программных компонентов {var(sys2)}.