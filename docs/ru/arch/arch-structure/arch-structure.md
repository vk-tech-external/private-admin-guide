# {heading(Функциональная структура)[id=arch_structure]}

Структура {var(sys2)} предусматривает разделение всего объёма функций на логические блоки — подсистемы.

В состав {var(sys2)} входят следующие функциональные компоненты:

* Интерфейсы:
   * Подсистема потребителя облачных услуг — позволяет пользователям {var(sys2)} создавать виртуальные вычислительные ресурсы и управлять ими в рамках своего проекта или проектов в соответствии с полномочиями в проекте или проектах.
   * Подсистема администрирования — позволяет администраторам {var(sys2)} выполнять операции по обслуживанию прикладного ПО и сервисов {var(sys2)}, настройке и диагностированию {var(sys2)}, анализу метрик и журналов работы компонентов {var(sys2)}.
   * Подсистема установки и управления конфигурацией — выполнение операций по инициализации и управлению оборудованием, а также установке, обновлению или конфигурированию {var(sys2)} администраторами {var(sys2)}.
* Общие подсистемы:
   * Подсистема тарификации и биллинга — функции сбора информации о потребляемых ресурсах и учёт использования потребляемых ресурсов {var(sys2)} с точностью до минуты по модели «Pay-As-You-Go» (биллинг по факту использования).
   * Подсистема управления доступом — функции идентификации, аутентификации и авторизации пользователей и всех смежных сервисов между собой.
   * Подсистема служебных хранилищ — обеспечение работы {var(sys2)}, включая:
      * Ведение каталога образов ВМ.
      * Хранение секретов и ключей.
      * Предоставление сервиса удалённого файлового хранилища.
* Группа подсистем IaaS:
   * Подсистема вычислительных ресурсов — управление вычислительными узлами {var(sys2)} и виртуальными машинами.
   * Подсистема хранения данных — оркестрация конечных решений по блочному хранению данных.
   * Сетевая подсистема — реализация и управление всеми аспектами виртуальной сетевой инфраструктуры {var(sys2)}.
* Группа подсистем PaaS/SaaS:
   * Подсистема управляемых баз данных — предназначена для автоматизации развёртывания СУБД в {var(sys3)}.
   * Подсистема управляемых контейнеров — предназначена для автоматизации управления кластерами контейнеров Kubernetes в {var(sys3)}.
   * Подсистема облачных приложений — предназначена для автоматизации развёртывания типовых приложений в {var(sys3)}.

Для реализации указанных подсистем используется совокупность множества программных компонентов {var(sys2)}.