# {heading(Функциональная структура)[id=arch_structure]}

<!--- #todo на раздел есть ссылка в ПМИ Private Cloud. -->

{ifdef(box, pg)}

<info>

Подробная информация о технических решениях и внутреннем устройстве {var(sys2)} приведена в документе **Руководство по проектированию {var(system)}**.

</info>

{/ifdef}

<!--- //#todo Синхронизировать с РПП. Возможно, большая часть раздела (или даже весь целиком) переедет туда -->

Структура {ifdef(box, pg)}{var(sys2)}{/ifdef}{ifndef(box, pg)}{var(sys2_go)}{/ifndef} предусматривает разделение всего объема функций на логические блоки — подсистемы. Перечень подсистем приведен в {linkto(#tab_arch_structure)[text=таблице %number]}.

{ifdef(box, pg)}
{caption(Таблица {counter(table)[id=numb_tab_arch_structure]} — Список подсистем {var(sys2)})[align=right;position=above;id=tab_arch_structure;number={const(numb_tab_arch_structure)}]}
[cols="1,3", options="header"]
|===
|Подсистема
|Описание

2+|**Группа подсистем IaaS**

|Подсистема виртуальных серверов Cloud Servers
|Предназначена для управления вычислительными ресурсами {var(sys2)}, виртуальными машинами, виртуальными дисками и файловыми хранилищами

|Сетевая подсистема Cloud Network
|Предназначена для обеспечения сетевого взаимодействия в рамках проекта

|Подсистема балансировщиков нагрузки Load Balancing
|Предназначена для обеспечения сервиса балансировки нагрузки

2+|**Группа подсистем PaaS**

|Подсистема управляемых контейнеров Cloud Containers
|Предназначена для создания и управления кластерами Kubernetes

|Подсистема управляемых баз данных Cloud Database
|Предназначена для создания и управления масштабируемыми СУБД

2+|**Группа подсистем SaaS**

|Подсистема магазина приложений Marketplace
|Предназначена для автоматизированного предоставления доступа к прикладным сервисам, развернутым в {var(sys3)}, по запросу пользователей

2+|**Общие подсистемы**

|Подсистема аутентификации и авторизации
|Предназначена для управления идентификацией, аутентификацией и авторизацией пользователей и сервисов {var(sys2)}

|Подсистема прикладного мониторинга Cloud Monitoring
|Предназначена для мониторинга полезной нагрузки в рамках проекта

|Подсистема прикладного журналирования Cloud Logging
|Предназначена для журналирования полезной нагрузки в рамках проекта

|Подсистема уведомлений Cloud Alerting
|Предназначена для настройки оповещений об изменении ключевых метрик в работе пользовательской нагрузки

|Подсистема аудита Cloud Audit
|Предназначена для сохранения и просмотра событий безопасности в рамках проекта

|Подсистема резервного копирования Cloud Backup
|Предназначена для управления планами резервного копирования ВМ и управляемых СУБД

|Подсистема Биллинг
|Предназначена для учета использования ресурсов и контроля расходов в рамках проектов, а также формирования финансовых отчетов
|===
{/caption}
{/ifdef}

{ifndef(box, pg)}
{caption(Таблица {counter(table)[id=numb_tab_arch_structure]} — Список подсистем {var(sys2_go)})[align=right;position=above;id=tab_arch_structure;number={const(numb_tab_arch_structure)}]}
[cols="1,3", options="header"]
|===
|Подсистема
|Краткое описание

|Подсистема управления доступом
|Предназначена для идентификации, аутентификации и авторизации пользователей и всех смежных сервисов между собой

|Подсистема вычислительных ресурсов
|Предназначена для управления вычислительными узлами {var(sys2_go)} и ВМ

|Подсистема программно-определяемой сети
|Предназначена для управления сетевой инфраструктурой

|Подсистема хранения образов
|Предназначена для хранения образов ВМ и метаданных

|Подсистема хранения данных
|Предназначена для оркестрации конечных решений по блочному хранению данных

|Подсистема резервного копирования
|Предназначена для создания копий ВМ и их метаданных для последующего восстановления

|Подсистема управления квотами
|Предназначена для управления, дистрибьюции и предоставления информации о текущих квотах для других подсистем платформы

|Подсистема управления биллингом
|Предназначена для сбора информации о потребляемых ресурсах и учета использования потребляемых ресурсов {var(sys2_go)} с точностью до минуты по модели «Pay-As-You-Go» (биллинг по факту использования)

|Подсистема интерфейса пользователя
|Предназначена для создания и управления пользователями {var(sys2_go)} виртуальными вычислительными ресурсами в рамках проекта или проектов

|Подсистема интерфейса администратора
|Предназначена для выполнения администратором {var(sys2_go)} операций по обслуживанию прикладного ПО и сервисов {var(sys2_go)}, настройке и диагностированию {var(sys2_go)}, анализу метрик и журналов работы компонентов {var(sys2_go)}

|Подсистема интерфейса командной строки
|Предназначена для предоставления интерфейса командной строки для работы пользователей и администраторов {var(sys2_go)} с объектами Подсистемы вычислительных ресурсов, Подсистемы программно-определяемой сети, Подсистемы хранения образов, Подсистемы хранения данных

|Подсистема балансировки
|Предназначена для объединения технических модулей {var(sys2_go)}, обеспечивающих повышение отказоустойчивости {var(sys2_go)}

|Специальный модуль invgen
|Модуль invgen предназначен для создания первоначального inventory для установки {var(sys2_go)}

|Общесистемные привлекаемые компоненты
|Предназначены для выполнения общесистемных функций {var(sys2_go)}
|===
{/caption}
{/ifndef}

Для реализации указанных подсистем используется совокупность множества программных компонентов {ifdef(box, pg)}{var(sys2)}{/ifdef}{ifndef(box, pg)}{var(sys2_go)}{/ifndef}.