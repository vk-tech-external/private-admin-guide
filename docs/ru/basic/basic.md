# {heading(Общая информация)[id=basic]}

<info>

Перед изучением документа ознакомьтесь с составом, структурой и функциональными возможностями программного комплекса, приведенными в **Описании {ifdef(box, pg)}{var(system)}{/ifdef}{ifndef(box, pg)}{var(system_go)}{/ifndef}**.

</info>

{ifdef(box, pg)}
{var(system)} — это программный комплекс, предназначенный для построения частной облачной инфраструктуры с каталогом услуг, устанавливаемый на аппаратные мощности, предоставляемые Заказчиком в собственном ЦОДе (On-premises).
{/ifdef}

{ifdef(cer)}
{var(system_go)} — это программный комплекс, предназначенный для построения частной облачной инфраструктуры с каталогом услуг, устанавливаемый на аппаратные мощности, предоставляемые Заказчиком в собственном ЦОДе (On-premises).

В состав {var(sys2_go)} входят следующие функциональные компоненты:

* Подсистема управления доступом.
* Подсистема вычислительных ресурсов.
* Подсистема программно-определяемой сети.
* Подсистема хранения образов.
* Подсистема хранения данных.
* Подсистема резервного копирования.
* Подсистема управления квотами.
* Подсистема управления биллингом.
* Подсистема интерфейса пользователя.
* Подсистема интерфейса администратора.
* Подсистема интерфейса командной строки.
* Подсистема балансировки.
* Специальный модуль invgen.
* Общесистемные привлекаемые компоненты.
{/ifdef}

## {heading(Наименование и условное обозначение)[id=name_and_designation]}

Полное наименование системы: {var(system)} от VK.

Сокращенное наименование: {ifdef(box, pg)}{var(system)}{/ifdef}{ifndef(box, pg)}{var(system_go)}{/ifndef}.

Краткое наименование системы: {ifdef(box, pg)}{var(sys1)}{/ifdef}{ifndef(box, pg)}{var(sys1_go)}{/ifndef}.

## {heading(Требования к квалификации персонала)[id=personnel_qualification_requirements]}

Обязанности системного администратора при работе с {ifdef(box, pg)}{var(sys5)}{/ifdef}{ifndef(box, pg)}{var(sys5_go)}{/ifndef}:

* Настройка и диагностирование {ifdef(box, pg)}{var(sys2)}{/ifdef}{ifndef(box, pg)}{var(sys2_go)}{/ifndef}.
* Обслуживание системного и прикладного программного обеспечения {ifdef(box, pg)}{var(sys2)}{/ifdef}{ifndef(box, pg)}{var(sys2_go)}{/ifndef}.
* Администрирование служебных баз данных {ifdef(box, pg)}{var(sys2)}{/ifdef}{ifndef(box, pg)}{var(sys2_go)}{/ifndef}.
* Резервное копирование и восстановление данных {ifdef(box, pg)}{var(sys2)}{/ifdef}{ifndef(box, pg)}{var(sys2_go)}{/ifndef}.
* Производство регламентных работ и анализ их результатов.

{ifdef(box, pg)}

<warn>

При возникновении неисправностей в работе {var(sys2)} обращайтесь в {linkto(../technical_support#technical_support)[text=группу технической поддержки]}.

</warn>

{/ifdef}

Требования, предъявляемые к системному администратору:

* Знание архитектуры компонентов OpenStack и Ceph.
* Экспертные знания в эксплуатации операционных систем семейства Linux.
* Знание интерфейса командной строки Bash.
* Знание инструментов автоматизации Ansible.
* Умение программировать на алгоритмическом языке Python (опционально).
* Понимание принципов функционирования облачных и интернет-сервисов, их масштабирования, и построения отказоустойчивых систем.
* Опыт работы с программным обеспечением для автоматизации развертывания и управления приложениями в средах с поддержкой контейнеризации Docker.
{ifdef(box, pg)}
* Опыт работы с программным обеспечением для оркестрации контейнеризированных приложений Kubernetes.
{/ifdef}
* Понимание принципов работы сетевых протоколов: TCP/IP, DNS, HTTP, BGP (динамическая маршрутизация).
* Понимание принципов работы систем и протоколов хранения данных: FC, NFS, Ceph, S3.


{ifdef(cer)}
## {heading(Требования к приемке)[id=acceptance_requirements]}

Приемка {var(sys_go)} осуществляется в соответствии с документом **Технические условиями {var(sys_go)}. RU.19678294.00006-02 ТУ 01**.

## {heading(Установка)[id=install]}

Установка {var(sys_go)} выполняется в соответствии с документом **Руководство по установке {var(sys_go)}. RU.19678294.00006-02 РУ 01**.
{/ifdef}