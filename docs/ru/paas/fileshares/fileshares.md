# {heading(Файловое хранилище)[id=fileshares]}

Сервис **Файловое хранилище** — набор служб для управления файловыми хранилищами на {var(sys3)}. Позволяет создать удаленную файловую систему, смонтировать файловую систему на ВМ, а затем обеспечить обмен данными между ними.

Файловые хранилища можно подключить к инстансам проекта по протоколам `CIFS` или `NFS`, с разделением доступа по IP-адресу клиента (подробнее — в [официальной документации](https://mcs.mail.ru/docs/ru/base/iaas/fs/connect-fs)).

<warn>

Доступ к файловым хранилищам осуществляется только из ВМ внутри {var(sys2)}.

</warn>

## {heading(Операции с файловым хранилищем)[id=file_storage_operations]}

<!--- // при выполнении cli-команд возникает ошибка 'NoneType' object has no attribute '__getitem__', если не добавить адрес стенда — админы должны добавить вручную (инфо от Константина Нифанина) -->

<!--- // Пытался добавить адрес стенда сам. Ошибка та же самая возникает. Проверить пока не могу -->

Для корректной работы с файловым хранилищем через OpenStack CLI в файле `/usr/lib/python2.7/site-packages/manilaclient/common/httpclient.py` на управляющем узле укажите значение атрибута `base_url`, равное используемому стенду.

{caption(Значение по умолчанию)[align=left;position=above]}
```yml
base_url = '/'.join(url.split('/')[:3]) + '/'
```
{/caption}

{caption(Пример заменяемого значения)[align=left;position=above]}
```yml
base_url = 'https://overcloud111.devmail.ru/infra/share/v2/'
```
{/caption}

### {heading(Просмотр списка файловых хранилищ)[id=file_storage_list]}

Чтобы просмотреть список файловых хранилищ, используйте один из интерфейсов:

* Портал самообслуживания (подробнее — в документе **Руководство пользователя {var(system)}** в разделе **Облачные вычисления** → **Файловое хранилище**).

<!--- //* Портала администратора. -->

* OpenStack CLI.

<!---
==== Портал администратора

Чтобы просмотреть список файловых хранилищ через Портал администратора, перейдите в раздел *PAAS* → *Shares*. Список файловых хранилищ отобразится в таблице.

Чтобы посмотреть список файловых хранилищ, доступных проекту или созданных в нем, введите ID проекта в поле *project_id* или название проекта в поле *project_name*.

[NOTE]
====
Чтобы отобразить значения поля *project_id*, если они скрыты:

. Нажмите на иконку *Столбцы таблицы* слева от строки поиска.
. Установите флажок для *project_id*.
. Нажмите кнопку *Применить*. В столбце *project_id* отобразятся идентификаторы проектов.
====
-->

#### {heading(OpenStack CLI)[id=file_storage_list_cli]}

Чтобы просмотреть список файловых хранилищ с помощью OpenStack CLI:

1. Выполните подготовительные операции (подробнее — в разделе {linkto(../../interfaces_access#use_openstackcli_admin)[text=%text]}).
1. Выполните команду, описанную в разделе Справочника {linkto(../../cli_commands#cli_manila_list)[text=%text]}.

### {heading(Просмотр списка сетей файловых хранилищ)[id=file_storage_network_list]}

Чтобы просмотреть список сетей файловых хранилищ, используйте один из интерфейсов:

* Портал самообслуживания (подробнее — в документе **Руководство пользователя {var(system)}** в разделе **Облачные вычисления** → **Файловое хранилище**).

<!--- //* Портала администратора. -->

* OpenStack CLI.

<!---
==== Портал администратора

Чтобы просмотреть список сетей файловых хранилищ через Портал администратора:

. Перейдите в раздел *PAAS* → *Shares*.
. Нажмите на иконку *Столбцы таблицы* слева от строки поиска.
. Установите флажок для значения *share_network_id*.
. Нажмите кнопку *Применить*. В столбце *share_network_id* отобразятся идентификаторы сетей файловых хранилищ.
-->

#### {heading(OpenStack CLI)[id=file_storage_network_list_cli]}

Чтобы просмотреть список сетей файловых хранилищ с помощью OpenStack CLI:

1. Выполните подготовительные операции (подробнее — в разделе {linkto(../../interfaces_access#use_openstackcli_admin)[text=%text]}).
1. Выполните команду, описанную в разделе Справочника {linkto(../../cli_commands#cli_manila_share_network_list)[text=%text]}.

### {heading(Просмотр информации о файловом хранилище)[id=file_storage_info]}

Чтобы просмотреть информацию о файловом хранилище, используйте один из интерфейсов:

* Портал самообслуживания (подробнее — в документе **Руководство пользователя {var(system)}** в разделе **Облачные вычисления** → **Файловое хранилище**).
* OpenStack CLI.

#### {heading(OpenStack CLI)[id=file_storage_info_cli]}

Чтобы просмотреть информацию о файловом хранилище с помощью OpenStack CLI:

1. Выполните подготовительные операции (подробнее — в разделе {linkto(../../interfaces_access#use_openstackcli_admin)[text=%text]}).
1. Выполните команду, описанную в разделе Справочника {linkto(../../cli_commands#cli_manila_show)[text=%text]}.

### {heading(Создание файлового хранилища)[id=file_storage_create]}

Чтобы создать файловое хранилище, используйте один из интерфейсов:

* Портал самообслуживания (подробнее — в документе **Руководство пользователя {var(system)}** в разделе **Облачные вычисления** → **Файловое хранилище** → **Создание файлового хранилища**).
* OpenStack CLI.

#### {heading(OpenStack CLI)[id=file_storage_create_cli]}

Чтобы создать файловое хранилище с помощью OpenStack CLI:

1. Выполните подготовительные операции (подробнее — в разделе {linkto(../../interfaces_access#use_openstackcli_admin)[text=%text]}).
1. Выполните команду, описанную в разделе Справочника {linkto(../../cli_commands#cli_manila_create)[text=%text]}.

### {heading(Увеличение размера файлового хранилища)[id=file_storage_size_increase]}

Чтобы увеличить размер файлового хранилища, используйте один из интерфейсов:

* Портал самообслуживания (подробнее — в документе **Руководство пользователя {var(system)}** в разделе **Облачные вычисления** → **Файловое хранилище** → **Управление файловыми хранилищами** → **Портал самообслуживания** → **Изменение размера файлового хранилища**).
* OpenStack CLI.

#### {heading(OpenStack CLI)[id=file_storage_size_increase_cli]}

Чтобы увеличить размер файлового хранилища с помощью OpenStack CLI:

1. Выполните подготовительные операции (подробнее — в разделе {linkto(../../interfaces_access#use_openstackcli_admin)[text=%text]})
1. Выполните команду, описанную в разделе Справочника {linkto(../../cli_commands#cli_manila_extend)[text=%text]}.

### {heading(Уменьшение размера файлового хранилища)[id=file_storage_size_decrease]}

Чтобы уменьшить размер файлового хранилища с помощью OpenStack CLI:

1. Выполните подготовительные операции (подробнее — в разделе {linkto(../../interfaces_access#use_openstackcli_admin)[text=%text]}).
1. Выполните команду, описанную в разделе Справочника {linkto(../../cli_commands#cli_manila_shrink)[text=%text]}.

### {heading(Удаление файлового хранилища)[id=file_storage_delete]}

Чтобы удалить файловое хранилище, используйте один из интерфейсов:

* Портал самообслуживания (подробнее — в документе **Руководство пользователя {var(system)}** в разделе **Облачные вычисления** → **Файловое хранилище** → **Управление файловыми хранилищами** → **Портал самообслуживания** → **Удаление файлового хранилища**).
* OpenStack CLI.

#### {heading(OpenStack CLI)[id=file_storage_delete_cli]}

Чтобы удалить файловое хранилище с помощью OpenStack CLI:

1. Выполните подготовительные операции (подробнее — в разделе {linkto(../../interfaces_access#use_openstackcli_admin)[text=%text]}).
1. Выполните команду, описанную в разделе Справочника {linkto(../../cli_commands#cli_manila_delete)[text=%text]}.

## {heading(Правила доступа для файлового хранилища)[id=file_storage_access]}

### {heading(Просмотр списка правил доступа)[id=file_storage_access_list]}

Чтобы просмотреть список правил доступа файлового хранилища, используйте один из интерфейсов:

* Портал самообслуживания (подробнее — в документе **Руководство пользователя {var(system)}** в разделе **Облачные вычисления** → **Файловое хранилище** → **Управление файловыми хранилищами** → **Портал самообслуживания** → **Просмотр списка правил доступа**).
* OpenStack CLI.

#### {heading(OpenStack CLI)[id=file_storage_access_list_cli]}

Чтобы просмотреть список правил доступа файлового хранилища с помощью OpenStack CLI:

1. Выполните подготовительные операции (подробнее — в разделе {linkto(../../interfaces_access#use_openstackcli_admin)[text=%text]}).
1. Выполните команду, описанную в разделе Справочника {linkto(../../cli_commands#cli_manila_access_list)[text=%text]}.

### {heading(Добавление правила доступа)[id=file_storage_access_allow]}

<warn>

При использовании протокола CIFS, права доступа предоставляются только на **Чтение и запись**.

</warn>

Чтобы добавить правило доступа файлового хранилища, используйте один из интерфейсов:

* Портал самообслуживания (подробнее — в документе **Руководство пользователя {var(system)}** в разделе **Облачные вычисления** → **Файловое хранилище** → **Управление файловыми хранилищами** → **Портал самообслуживания** → **Добавление правил доступа**).
* OpenStack CLI.

#### {heading(OpenStack CLI)[id=file_storage_access_allow_cli]}

Чтобы добавить правило доступа файлового хранилища с помощью OpenStack CLI:

1. Выполните подготовительные операции (подробнее — в разделе {linkto(../../interfaces_access#use_openstackcli_admin)[text=%text]}).
1. Выполните команду, описанную в разделе Справочника {linkto(../../cli_commands#cli_manila_access_allow)[text=%text]}.

### {heading(Удаление правила доступа)[id=file_storage_access_deny]}

Чтобы удалить правило доступа файлового хранилища, используйте один из интерфейсов:

* Портал самообслуживания (подробнее — в документе **Руководство пользователя {var(system)}** в разделе **Облачные вычисления** → **Файловое хранилище** → **Управление файловыми хранилищами** → **Портал самообслуживания** → **Удаление правил доступа**).
* OpenStack CLI.

#### {heading(OpenStack CLI)[id=file_storage_access_deny_cli]}

Чтобы удалить правило доступа файлового хранилища с помощью OpenStack CLI:

1. Выполните подготовительные операции (подробнее — в разделе {linkto(../../interfaces_access#use_openstackcli_admin)[text=%text]}).
1. Выполните команду, описанную в разделе Справочника {linkto(../../cli_commands#cli_manila_access_deny)[text=%text]}.

## {heading(Снимки (снэпшоты) файлового хранилища)[id=file_storage_snapshot]}

### {heading(Просмотр списка снимков)[id=file_storage_snapshot_list]}

Чтобы просмотреть список снимков файлового хранилища, используйте один из интерфейсов:

* Портал самообслуживания (подробнее — в документе **Руководство пользователя {var(system)}** в разделе **Облачные вычисления** → **Файловое хранилище** → **Управление снимками файлового хранилища** → **Портал самообслуживания** → **Просмотр снимков файлового хранилища**).
* OpenStack CLI.

#### {heading(OpenStack CLI)[id=file_storage_snapshot_list_cli]}

Чтобы просмотреть список снимков файлового хранилища с помощью OpenStack CLI:

1. Выполните подготовительные операции (подробнее — в разделе {linkto(../../interfaces_access#use_openstackcli_admin)[text=%text]}).
1. Выполните команду, описанную в разделе Справочника {linkto(../../cli_commands#cli_manila_snapshot_list)[text=%text]}.

### {heading(Создание снимка файлового хранилища)[id=file_storage_snapshot_create]}

Чтобы создать снимок файлового хранилища, используйте один из интерфейсов:

* Портал самообслуживания (подробнее — в документе **Руководство пользователя {var(system)}** в разделе **Облачные вычисления** → **Файловое хранилище** → **Управление снимками файлового хранилища** → **Портал самообслуживания** → **Создание снимка файлового хранилища**).
* OpenStack CLI.

#### {heading(OpenStack CLI)[id=file_storage_snapshot_create_cli]}

Чтобы создать снимок файлового хранилища с помощью OpenStack CLI:

1. Выполните подготовительные операции (подробнее — в разделе {linkto(../../interfaces_access#use_openstackcli_admin)[text=%text]}).
1. Выполните команду, описанную в разделе Справочника {linkto(../../cli_commands#cli_manila_snapshot_create)[text=%text]}.

### {heading(Удаление снимка файлового хранилища)[id=file_storage_snapshot_delete]}

Чтобы удалить снимок файлового хранилища, используйте один из интерфейсов:

* Портал самообслуживания (подробнее — в документе **Руководство пользователя {var(system)}** в разделе **Облачные вычисления** → **Файловое хранилище** → **Управление снимками файлового хранилища** → **Портал самообслуживания** → **Удаление снимка файлового хранилища**).
* OpenStack CLI.

#### {heading(OpenStack CLI)[id=file_storage_snapshot_delete_cli]}

Чтобы удалить снимок файлового хранилища с помощью OpenStack CLI:

1. Выполните подготовительные операции (подробнее — в разделе {linkto(../../interfaces_access#use_openstackcli_admin)[text=%text]}).
1. Выполните команду, описанную в разделе Справочника {linkto(../../cli_commands#cli_manila_snapshot_delete)[text=%text]}.
